version: '3.8'

services:
  app:
    image: lionmq:latest  # 使用 lionmq 的映像
    ports:
      - "3002:3002"
    environment:
      - PORT=3002
      - SERVER_HOST=http://10.101.4.132:3002
      - WATCHER_SERVER=ws://10.101.4.132:3008
      - MONGODB_SERVER=MONGODB_SERVER=mongodb+srv://Aaren:3030416@cluster0.mollxex.mongodb.net/?retryWrites=true&w=majority&tls=true&tlsAllowInvalidCertificates=true
      - MONGODB_DOCUMENT=LionMQ_node1
    depends_on:
      - mongo
    volumes:
      - .:/app

  mongo:
    image: mongo:latest
    ports:
      - "27018:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
